# DevOps_Sandbox

This is a project that use Docker container to simulate server such as digital ocean or AWS EC2 
and use Prometheus to monitor RAM and CPU then use Apache Bench to query from server to see
the difference between Normalized database and Denormalized database to get an appropriate spec.
